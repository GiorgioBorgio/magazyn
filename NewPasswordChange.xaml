<Window x:Class="Magazyn.NewPasswordChange"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zmiana hasła" Height="250" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nowe hasło:" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
        <PasswordBox x:Name="NewPasswordBox" Grid.Row="1" Margin="0,0,0,15" Height="30" />

        <TextBlock Text="Powtórz hasło:" Grid.Row="2" VerticalAlignment="Center" Margin="0,0,0,5"/>
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="3" Margin="0,0,0,15" Height="30" />

        <Button Content="Zapisz" Grid.Row="4" Height="35" Width="100" HorizontalAlignment="Center" Click="SaveButton_Click"/>
    </Grid>
</Window>
