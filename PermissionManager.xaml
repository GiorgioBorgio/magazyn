<UserControl x:Class="Magazyn.PermissionManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Magazyn"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

        <!-- Styl dla DataGridRow z wyłączonym podświetlaniem po kliknięciu -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8F0"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Styl dla DataGridCell z wyłączonym zaznaczeniem -->
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="White"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Styl przycisku -->
        <Style x:Key="ModifyButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" SnapsToDevicePixels="True">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5,0">
                                <TextBlock Text="👤" FontSize="14" Margin="0,0,6,0"/>
                                <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFD580"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid x:Name="PermissionManager1" Loaded="PermissionManager1_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#FF1E293B" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="👥 Zarządzanie uprawnieniami"
                           FontSize="18"
                           Foreground="White"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="23,0,0,0"/>

                <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,20,0">
                    <Button Content="↩ Powrót"
                            Margin="0,10,0,10"
                            Width="100" Height="30"
                            Background="#94A3B8"
                            Foreground="White"
                            Cursor="Hand"
                            Click="BackButton_Click"
                            BorderThickness="0" FontSize="14" FontWeight="Bold">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="8"
                                        SnapsToDevicePixels="True">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Margin="0,20,20,0"/>
        <Border CornerRadius="12" Margin="40,0,125,282" Padding="20" Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Top" HorizontalAlignment="Left">
                    <Border Background="#E2E8F0" 
                            BorderBrush="#CBD5E1" 
                            BorderThickness="1"
                            CornerRadius="6" 
                            Height="38"
                            Width="270" 
                            VerticalAlignment="Top">
                        <Grid Margin="10,0">
                            <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center"
                                      Margin="0,0,6,0" Foreground="#334155" HorizontalAlignment="Left"/>

                            <TextBlock x:Name="PlaceholderText"
                                       Text="Wyszukaj"
                                       FontSize="13"
                                       Foreground="Gray"
                                       Margin="24,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Visible"/>

                            <TextBox x:Name="TextBoxSearch"
                                     Width="200"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     Foreground="#1E293B"
                                     FontSize="13"
                                     VerticalContentAlignment="Center"
                                     Margin="24,0,0,0"
                                     Text=""
                                     TextChanged="TextBoxSearch_TextChanged"/>
                        </Grid>
                    </Border>
                    <ComboBox x:Name="SearchTypeComboBox" 
                              SelectedIndex="0"
                              DockPanel.Dock="Right" Width="128" FontSize="14">
                        <ComboBoxItem Tag="Name" Content="Imię i Nazwisko"/>
                        <ComboBoxItem Tag="Login" Content="Login"/>
                        <ComboBoxItem Tag="E-mail" Content="Uprawnienie"/>
                    </ComboBox>
                </DockPanel>

                <DataGrid x:Name="UserDataGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserReorderColumns="False"
                          CanUserSortColumns="False"
                          RowHeight="60"
                          RowHeaderWidth="0"
                          GridLinesVisibility="None"
                          BorderThickness="0"
                          AlternatingRowBackground="White"
                          RowBackground="White"
                          ColumnHeaderHeight="40"
                          EnableRowVirtualization="False"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          Background="Transparent"  
                          Margin="0,0,0,-56"
                          FontWeight="Bold"
                          BorderBrush="#FF688CAF"
                          CanUserResizeColumns="False"
                          Grid.ColumnSpan="2">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}" Width="*" />
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName}" Width="*" />
                        <DataGridTextColumn Header="Login" Binding="{Binding Login}" Width="*" />
                        <DataGridTemplateColumn Header="Uprawnienia" Width="4*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="1" BorderBrush="#CBD5E1" CornerRadius="4" Padding="4" Background="#F8FAFC">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="50">
                                            <ItemsControl ItemsSource="{Binding UserPermissions}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Permission.Name}" FontSize="12" Foreground="#1E293B" />
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>


                        <DataGridTemplateColumn Header="Akcje" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Button Content="Zarządzaj"
                                                Style="{StaticResource ModifyButtonStyle}"
                                                Tag="{Binding}"
                                                Click="button_zarzadzaj_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
