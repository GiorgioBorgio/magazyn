<Window x:Class="Magazyn.UsersMenager"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Management" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="14"
        Background="#F2F4F7">

    <Window.Resources>
        <!-- Kolory -->
        <SolidColorBrush x:Key="DarkGreen" Color="#134E4A"/>
        <SolidColorBrush x:Key="LightBackground" Color="White"/>
        <SolidColorBrush x:Key="TextMain" Color="#1E293B"/>
        <SolidColorBrush x:Key="BadgeGreen" Color="#22C55E"/>
        <SolidColorBrush x:Key="BadgeYellow" Color="#FACC15"/>
        <SolidColorBrush x:Key="BadgeGray" Color="#94A3B8"/>
        <SolidColorBrush x:Key="BadgeRed" Color="#EF4444"/>

        <!-- Styl zaokrąglonego przycisku -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BadgeYellow}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl komórek DataGrid -->
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,0,0,0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Styl wierszy DataGrid -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                            <DataGridCellsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl nagłówków kolumn DataGrid -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,4"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="SeparatorVisibility" Value="Collapsed"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Pasek górny -->
        <Border Background="{DynamicResource TextMain}" Height="60" VerticalAlignment="Top">
            <DockPanel Margin="20,0" LastChildFill="False">
                <TextBlock Text="👥 User Management" FontSize="18" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Name="ButtonAddUser"
                        Content="Add User"
                        VerticalAlignment="Center"
                        Width="100" Height="30"
                        Style="{StaticResource RoundedButtonStyle}"
                        DockPanel.Dock="Right"
                        Background="#22C55E" Foreground="White" Click="ButtonAddUser_Click"/>
            </DockPanel>
        </Border>

        <!-- Główna zawartość -->
        <Border Background="{StaticResource LightBackground}" CornerRadius="12" Margin="40,80,40,20" Padding="20">
            <Grid>
                <DataGrid x:Name="UserDataGrid"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserReorderColumns="False"
                          CanUserSortColumns="False"
                          RowHeight="60"
                          RowHeaderWidth="0"
                          GridLinesVisibility="None"
                          BorderThickness="0"
                          AlternatingRowBackground="White"
                          RowBackground="White"
                          ColumnHeaderHeight="40"
                          EnableRowVirtualization="False"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          HorizontalAlignment="Stretch"
                          Background="Transparent">
                    <DataGrid.Columns>
                        <!-- Kolumna: Name -->
                        <DataGridTextColumn Header=" Name" Binding="{Binding Name}" Width="2*" MinWidth="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Margin" Value="12,0,0,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Kolumna: Email -->
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*" MinWidth="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Margin" Value="12,0,0,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Kolumna: User Role -->
                        <DataGridTemplateColumn Header="User Role" Width="1.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                                        <Border Background="{StaticResource BadgeYellow}" CornerRadius="10" Padding="6,2" Margin="0,0,5,0">
                                            <TextBlock Text="Manager" Foreground="White" FontSize="12"/>
                                        </Border>
                                        <Border Background="{StaticResource BadgeGreen}" CornerRadius="10" Padding="6,2">
                                            <TextBlock Text="Admin" Foreground="White" FontSize="12"/>
                                        </Border>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Kolumna: Status -->
                        <DataGridTemplateColumn Header="Status" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource TextMain}" FontWeight="SemiBold" Margin="12,0,0,0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Kolumna: Actions -->
                        <DataGridTemplateColumn Header="Actions" Width="1.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                                        <Button Name ="ButtonModify" Content="Modify" FontSize="12" Padding="6,2" Margin="0,0,5,0"
                                                Style="{StaticResource RoundedButtonStyle}" Click="ButtonModify_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
