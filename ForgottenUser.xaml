<UserControl x:Class="Magazyn.ForgottenUser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Magazyn"
             mc:Ignorable="d"
             Background="#F2F4F7">
    <UserControl.Resources>
        <SolidColorBrush x:Key="DarkGreen" Color="#134E4A"/>
        <SolidColorBrush x:Key="LightBackground" Color="White"/>
        <SolidColorBrush x:Key="TextMain" Color="#1E293B"/>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,0,0,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                            <DataGridCellsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,4"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="SeparatorVisibility" Value="Collapsed"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border Background="{DynamicResource TextMain}" Height="60" VerticalAlignment="Top">
            <DockPanel Margin="20,0" LastChildFill="False">
                <TextBlock Text="📋 Lista użytkowników zapomnianych" 
                           FontSize="18" 
                           Foreground="White" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <Border Background="{StaticResource LightBackground}" CornerRadius="12" Margin="40,80,40,20" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Top" HorizontalAlignment="Left">
                    <Border Background="#E2E8F0" BorderBrush="#CBD5E1" BorderThickness="1"
                            CornerRadius="6" Height="38" Width="270">
                        <Grid Margin="10,0">
                            <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center"
                                       Margin="0,0,6,0" Foreground="#334155" HorizontalAlignment="Left"/>
                            <TextBlock x:Name="PlaceholderText" Text="Wyszukaj" FontSize="13" Foreground="Gray"
                                       Margin="24,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Visible"/>
                            <TextBox Name="TextBoxSearch" Width="200" BorderThickness="0" Background="Transparent"
                                     Foreground="#1E293B" FontSize="13" VerticalContentAlignment="Center"
                                     Margin="24,0,0,0" TextChanged="TextBoxSearch_TextChanged" Text=""/>
                        </Grid>
                    </Border>
                </DockPanel>

                <DataGrid x:Name="ForgottenDataGrid" Grid.Row="1" AutoGenerateColumns="False" HeadersVisibility="Column" IsReadOnly="True"
                          CanUserAddRows="False" CanUserReorderColumns="False" CanUserSortColumns="False"
                          RowHeight="60" RowHeaderWidth="0" GridLinesVisibility="None" BorderThickness="0"
                          AlternatingRowBackground="White" RowBackground="White" ColumnHeaderHeight="40"
                          EnableRowVirtualization="False" SelectionMode="Single" SelectionUnit="FullRow"
                          Background="Transparent">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Identyfikator" Binding="{Binding Id}" Width="*" />
                        <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}" Width="*" />
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName}" Width="*" />
                        <DataGridTextColumn Header="Data zapomnienia" Binding="{Binding ForgottenDate, StringFormat='dd.MM.yyyy HH:mm'}" Width="*" />
                        <DataGridTextColumn Header="Zapomniany przez" Binding="{Binding ForgottenById}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
