<UserControl
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="Magazyn.UsersManager"
        FontFamily="Segoe UI" FontSize="14"
        Background="#F2F4F7">

    <UserControl.Resources>
        <!-- Kolory -->
        <SolidColorBrush x:Key="DarkGreen" Color="#134E4A"/>
        <SolidColorBrush x:Key="LightBackground" Color="White"/>
        <SolidColorBrush x:Key="TextMain" Color="#1E293B"/>
        <SolidColorBrush x:Key="BadgeGreen" Color="#22C55E"/>
        <SolidColorBrush x:Key="BadgeYellow" Color="#FACC15"/>
        <SolidColorBrush x:Key="BadgeGray" Color="#94A3B8"/>
        <SolidColorBrush x:Key="BadgeRed" Color="#EF4444"/>

        <!-- Styl zaokrąglonego przycisku -->
        <Style x:Key="RoundedButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource BadgeYellow}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl przycisku Modify -->
        <Style x:Key="ModifyButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" SnapsToDevicePixels="True">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5,0">
                                <TextBlock Text="👤" FontSize="14" Margin="0,0,6,0"/>
                                <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8F0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#CBD5E1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Styl przycisku Zapomnij -->
        <Style x:Key="ForgetButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource BadgeRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" SnapsToDevicePixels="True">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5,0">
                                <TextBlock Text="🗑" FontSize="14" Margin="0,0,6,0"/>
                                <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#B91C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Styl komórek DataGrid -->
        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,0,0,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>



        <!-- Styl wierszy DataGrid -->
        <Style TargetType="{x:Type DataGridRow}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridRow}">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                            <DataGridCellsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Styl nagłówków kolumn DataGrid -->
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,4"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="SeparatorVisibility" Value="Collapsed"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Pasek górny -->
        <Border Background="{DynamicResource TextMain}" Height="60" VerticalAlignment="Top">
            <DockPanel Margin="20,0" LastChildFill="False">
                <!-- Tytuł -->
                <TextBlock Text="👥 Zarządzanie użytkownikami"
                   FontSize="18"
                   Foreground="White"
                   FontWeight="Bold"
                   VerticalAlignment="Center"
                   DockPanel.Dock="Left"/>

                <!-- Przyciski po prawej stronie -->
                <StackPanel Orientation="Horizontal"
            DockPanel.Dock="Right"
            VerticalAlignment="Center"
            HorizontalAlignment="Right"
            Margin="0,0,10,0"
            >
                    <!-- Zapomniani użytkownicy -->
                    <Button Content="Zapomniani użytkownicy"
            Width="180" Height="30"
            Margin="0,0,10,0"
            Style="{StaticResource RoundedButtonStyle}"
            Background="#f56b1b"
            Foreground="White"
            Click="ShowForgottenUsers_Click"/>

                    <!-- Dodaj -->
                    <Button x:Name="ButtonAddUser"
            Content="Dodaj"
            Width="100" Height="30"
            Margin="0,0,10,0"
            Style="{StaticResource RoundedButtonStyle}"
            Background="#22C55E"
            Foreground="White"
            Click="ButtonAddUser_Click"/>

                    <!-- Powrót -->
                    <Button Content="↩ Powrót"
            Width="100" Height="30"
            Margin="0,0,0,0"
            Style="{StaticResource RoundedButtonStyle}"
            Background="#94A3B8"
            Foreground="White"
            Click="ButtonReturn_Click"/>
                </StackPanel>

            </DockPanel>
        </Border>



        <!-- Główna zawartość -->
        <Border Background="{StaticResource LightBackground}" CornerRadius="12" Margin="40,80,40,144" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Wyszukiwarka -->
                    <RowDefinition Height="*"/>
                    <!-- DataGrid -->
                </Grid.RowDefinitions>

                <!-- Wyszukiwarka (przyciemniona) -->
                <DockPanel Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Top" HorizontalAlignment="Left">
                    <Border Background="#E2E8F0" 
                            BorderBrush="#CBD5E1" 
                            BorderThickness="1"
                            CornerRadius="6" 
                            Height="38"
                            Width="270" 
                            VerticalAlignment="Top">
                        <Grid Margin="10,0">
                            <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center"
                       Margin="0,0,6,0" Foreground="#334155" HorizontalAlignment="Left"/>

                            <!-- Placeholder -->
                            <TextBlock x:Name="PlaceholderText"
                                       Text="Wyszukaj"
                                       FontSize="13"
                                       Foreground="Gray"
                                       Margin="24,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                       Visibility="Visible"/>

                            <TextBox x:Name="TextBoxSearch"
                                     Width="200"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     Foreground="#1E293B"
                                     FontSize="13"
                                     VerticalContentAlignment="Center"
                                     Margin="24,0,0,0"
                                     TextChanged="TextBoxSearch_TextChanged"
                     Text=""/>
                        </Grid>
                    </Border>
                    <ComboBox x:Name="SearchTypeComboBox" 
              SelectedIndex="0"
              DockPanel.Dock="Right" Width="128">
                        <ComboBoxItem Tag="Name" Content="Imię i Nazwisko"/>
                        <ComboBoxItem Tag="Login" Content="Login"/>
                        <ComboBoxItem Tag="Email" Content="Email"/>
                    </ComboBox>
                </DockPanel>




                <!-- DataGrid -->
                <DataGrid x:Name="UserDataGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserReorderColumns="False"
                          CanUserSortColumns="False"
                          RowHeight="60"
                          RowHeaderWidth="0"
                          GridLinesVisibility="None"
                          BorderThickness="0"
                          AlternatingRowBackground="White"
                          RowBackground="White"
                          ColumnHeaderHeight="40"
                          EnableRowVirtualization="False"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          Background="Transparent"  Margin="0,0,0,-56">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}" Width="*" />
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName}" Width="*" />
                        <DataGridTextColumn Header="Login" Binding="{Binding Login}" Width="*" />
                        <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="2*" />
                        <DataGridTextColumn Header="Role" Binding="{Binding UserPermission}" Width="*" />
                        <DataGridTemplateColumn Header="Akcje" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <Button Content="Podgląd"
                                                Click="ButtonPreview_Click"
                                                Style="{StaticResource ModifyButtonStyle}"
                                                Width="100"
                                                Height="40"
                                                Margin="0,0,10,0"
                                                Padding="10,4"/>
                                        <Button Content="Zapomnij"
                                                
                                                x:Name="buttonForget"
                                                Style="{StaticResource ForgetButtonStyle}"
                                                Width="100"
                                                Height="40"
                                                Padding="10,4" Click="buttonForget_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>



        <!--<ContentControl x:Name="ContentArea" Grid.Row="1"/>-->
        <ContentControl x:Name="ForgottenUserContent" Grid.Row="2" Visibility="Collapsed"/>
    </Grid>
</UserControl>